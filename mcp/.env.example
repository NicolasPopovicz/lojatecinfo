# URL da API do LojaTecInfo
# Fora do Docker: http://localhost:8080
# Dentro do Docker (rede interna): http://app
LOJA_API_URL=http://localhost:8080

# Token Sanctum — gere com:
# php artisan tinker
# >>> App\Models\User::first()->createToken('mcp')->plainTextToken
MCP_API_TOKEN=
